export interface Municipality{
    id: string;
    name: string;
    district_name: string;
}

export interface Product{
    id: string;
    title: string;
    image: string;
    category: string;
}

export interface Categoria{
    name: string;
}